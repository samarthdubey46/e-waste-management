import React from 'react'
import {View,Button,Text} from 'react-native'

const Info = ({navigation,route}) => {
    return(
        <View style={{flex:1}}>
			<Text>Info</Text>
            
        </View>
    )
}
export default Info