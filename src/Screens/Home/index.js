import React from 'react'
import {View,Button,Text} from 'react-native'

const Home = ({navigation,route}) => {
    return(
        <View style={{flex:1}}>
			<Text>Home</Text>
            
        </View>
    )
}
export default Home