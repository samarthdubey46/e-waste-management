import React from 'react'
import {View,Button,Text} from 'react-native'

const AddComplaint = ({navigation,route}) => {
    return(
        <View style={{flex:1}}>
			<Text>AddComplaint</Text>
            
        </View>
    )
}
export default AddComplaint